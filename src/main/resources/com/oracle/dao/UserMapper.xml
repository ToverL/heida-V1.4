<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.oracle.dao.UserDao">
    <insert id="saveUserInfo" parameterType="com.oracle.vo.User">
        insert into user (userid, headimg, nickname, articlesnum, likednum, commnum) values (#{userid},#{headimg},#{nickname},NULL ,NULL ,NULL );
    </insert>
    
</mapper>